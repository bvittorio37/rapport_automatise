{% extends 'base.html.twig' %}

{% block title %}CategorieMateriel index{% endblock %}

{% block body %}
    <h1>CategorieMateriel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Categorie</th>
                <th>Remarque</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_materiel in categorie_materiels %}
            <tr>
                <td>{{ categorie_materiel.id }}</td>
                <td>{{ categorie_materiel.categorie }}</td>
                <td>{{ categorie_materiel.remarque }}</td>
                <td>
                    <a href="{{ path('app_categorie_materiel_show', {'id': categorie_materiel.id}) }}">show</a>
                    <a href="{{ path('app_categorie_materiel_edit', {'id': categorie_materiel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_materiel_new') }}">Create new</a>
{% endblock %}
