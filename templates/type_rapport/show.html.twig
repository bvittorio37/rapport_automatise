{% extends 'base.html.twig' %}

{% block title %}TypeRapport{% endblock %}

{% block body %}
    <h1>TypeRapport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_rapport.id }}</td>
            </tr>
            <tr>
                <th>TypeRapport</th>
                <td>{{ type_rapport.typeRapport }}</td>
            </tr>
            <tr>
                <th>Remarque</th>
                <td>{{ type_rapport.remarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_rapport_index') }}">back to list</a>

    <a href="{{ path('app_type_rapport_edit', {'id': type_rapport.id}) }}">edit</a>

    {{ include('type_rapport/_delete_form.html.twig') }}
{% endblock %}
