{% extends 'base.html.twig' %}

{% block title %}Unite{% endblock %}

{% block body %}
    <h1>Unite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unite.id }}</td>
            </tr>
            <tr>
                <th>Unite</th>
                <td>{{ unite.unite }}</td>
            </tr>
            <tr>
                <th>Remarque</th>
                <td>{{ unite.remarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_unite_index') }}">back to list</a>

    <a href="{{ path('app_unite_edit', {'id': unite.id}) }}">edit</a>

    {{ include('unite/_delete_form.html.twig') }}
{% endblock %}
