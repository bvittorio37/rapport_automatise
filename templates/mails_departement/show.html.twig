{% extends 'base.html.twig' %}

{% block title %}MailsDepartement{% endblock %}

{% block body %}
    <h1>MailsDepartement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mails_departement.id }}</td>
            </tr>
            <tr>
                <th>NomEmp</th>
                <td>{{ mails_departement.nomEmp }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ mails_departement.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mails_departement_index') }}">back to list</a>

    <a href="{{ path('app_mails_departement_edit', {'id': mails_departement.id}) }}">edit</a>

    {{ include('mails_departement/_delete_form.html.twig') }}
{% endblock %}
