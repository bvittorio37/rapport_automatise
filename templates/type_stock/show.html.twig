{% extends 'base.html.twig' %}

{% block title %}TypeStock{% endblock %}

{% block body %}
    <h1>TypeStock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_stock.id }}</td>
            </tr>
            <tr>
                <th>TypeStock</th>
                <td>{{ type_stock.typeStock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_stock_index') }}">back to list</a>

    <a href="{{ path('app_type_stock_edit', {'id': type_stock.id}) }}">edit</a>

    {{ include('type_stock/_delete_form.html.twig') }}
{% endblock %}
