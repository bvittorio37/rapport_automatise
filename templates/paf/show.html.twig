{% extends 'base.html.twig' %}

{% block title %}Paf{% endblock %}

{% block body %}
    <h1>Paf</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paf.id }}</td>
            </tr>
            <tr>
                <th>Paf</th>
                <td>{{ paf.paf }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paf_index') }}">back to list</a>

    <a href="{{ path('app_paf_edit', {'id': paf.id}) }}">edit</a>

    {{ include('paf/_delete_form.html.twig') }}
{% endblock %}
